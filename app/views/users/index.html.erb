<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @users, renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer %>

<ul class="users mb-4">
    <%= render @users %>
</ul>

<%= will_paginate @users, renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer %>